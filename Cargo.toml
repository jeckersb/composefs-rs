[workspace]
members = ["crates/*"]
# Exclude integration-tests from default `cargo test` — those require a
# built cfsctl binary and (for privileged tests) a VM.  Run them via
# `just test-integration` or `just test-integration-vm` instead.
default-members = [
    "crates/composefs-ctl",
    "crates/composefs",
    "crates/composefs-boot",
    "crates/composefs-fuse",
    "crates/composefs-http",
    "crates/composefs-ioctls",
    "crates/composefs-oci",
    "crates/composefs-ostree",
    "crates/composefs-setup-root",
    "crates/composefs-storage",
    "crates/composefs-erofs-debug",
    "crates/composefs-splitdirfdstream",
]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/containers/composefs-rs"
rust-version = "1.88.0"
version = "0.8.0"

[workspace.lints.rust]
missing_docs = "deny"
missing_debug_implementations = "deny"
unsafe_code = "deny" # https://github.com/containers/composefs-rs/issues/123

[workspace.dependencies]
composefs = { version = "0.8.0", path = "crates/composefs", default-features = false }
composefs-ctl = { version = "0.8.0", path = "crates/composefs-ctl", default-features = false }
composefs-ioctls = { version = "0.8.0", path = "crates/composefs-ioctls", default-features = false }
composefs-oci = { version = "0.8.0", path = "crates/composefs-oci", default-features = false }
composefs-boot = { version = "0.8.0", path = "crates/composefs-boot", default-features = false }
composefs-fuse = { version = "0.8.0", path = "crates/composefs-fuse", default-features = false }
composefs-http = { version = "0.8.0", path = "crates/composefs-http", default-features = false }
composefs-ostree = { version = "0.8.0", path = "crates/composefs-ostree", default-features = false }
cap-std-ext = "5.1.2"
ocidir = "0.8.0"

zlink = { version = "0.7", default-features = false, features = ["tokio", "introspection", "proxy", "server", "service", "tracing"] }
zlink-core = { version = "0.7", default-features = false, features = ["introspection", "std", "tracing"] }

[profile.profiling]
inherits = "release"
debug = true

[workspace.metadata.vendor-filter]
# match cargo-vendor-filterer config from bootc
platforms = ["*-unknown-linux-gnu"]
tier = "2"
all-features = true
